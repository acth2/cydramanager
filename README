build:
 (Execute theses commands in the root of the project.)
 - 'meson setup build'
 - 'ninja -C build'
The compiled binary is located at the new build folder as cydramanager in order to install it in your system do
 - ''
